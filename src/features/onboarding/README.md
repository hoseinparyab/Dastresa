# Onboarding

Post-install welcome tour that introduces Dastresa features without auto-enabling the extension.

- Opens a dedicated page on first install (`chrome.runtime.onInstalled` reason `install`)
- Persists completion in `Dastresa.onboarding`
- Replay from Options; optional CTA in Popup until dismissed
